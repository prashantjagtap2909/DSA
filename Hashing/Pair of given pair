Check pair of given pair is present or not

bool isPair(int arr[],int n,int sum){
    unordered_set<int>s;
    for(int i 0 ;i<n;i++){
        if(s.find(sum-arr[i] ) != s.end();
            return true;
        else
            s.insert(arr[i]);
    }
    return false;
}
