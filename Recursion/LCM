Findng the LCM

int gcd(int a , int b){
    if(b==0)
        return a;
    else
        return gcd(a%b);
}

int lcm(int a , int b){
    return (a*b)/gcd(a,b)
}


here we have used to two find lcm. first gcd and use this value in lcm function.
