To check the given string is palindromic or not

bool isPalindromic(srting str,int start, int end){
    if(start>=end)
        return true;
    return (str[start]==str[end]) && isPalindromic(str,start+1,end-1);
}
